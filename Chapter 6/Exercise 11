#include <stdio.h>
#define SIZE 8
int main (void)
{
	int arr[SIZE], count;

	printf("Please enter 8 integers:\n");

	for (count = 0; count < SIZE; count++)
		scanf("%d", &arr[count]);
	for (count = SIZE - 1; count >= 0; count--)
		printf("%d", arr[count]); 

	return 0;
}
