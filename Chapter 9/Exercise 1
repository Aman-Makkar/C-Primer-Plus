#include <stdio.h>

double min(double x, double y);

int main (void)
{
	double x = 100, y = 1000;

	printf("%.2lf\n",min(x, y));

	return 0;
}

double min (double x, double y)
{
	double smallest;

	if (x < y)
		smallest = x;
	else
		smallest = y;

	return smallest;
}
