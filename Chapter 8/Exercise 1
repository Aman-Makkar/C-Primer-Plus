#include<stdio.h>
int main (void)
{
	int total = 0;
	int count = 0;
	char ch;

	printf("Enter as many characters as you like: ");

	while ((ch = getchar()) != EOF)
		count++;
	total += count;

	printf("%d\n", total);

	return 0;
}
